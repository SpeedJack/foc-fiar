#include "random.h"
#include "digest.h"

unsigned int gcm_encrypt(const char *plaintext, unsigned int plaintext_len, char *ciphertext)
{
}

unsigned int gcm_decrypt(const char *ciphertext, unsigned int ciphertext_len, char *decryptedtext)
{
}
